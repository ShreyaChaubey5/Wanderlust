<% layout('layouts/boilerplate') %>
<div class="row mt-3">
  <div class="col-8 offset-2">
      <h3>Listing details:</h3>
      </div>
        <div class="card col-6 offset-2">
          <img src="<%= listing.image.url %>" class="card-img-top show-image" alt="listing-image">
            <div class="card-body">
            <div class="card-text"><b><%= listing.title %></b><br>
            <%= listing.description %><br>
            <%= listing.price %><br>
            <%= listing.location %><br>
            <%= listing.country %><br>
            
            </p>
           </div>
        </div>

      <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark show">Edit</a>
      <br><br>
      <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
        <button class="btn btn-dark show">Delete</button>
      </form>
 
</div>